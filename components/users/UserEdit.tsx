import styles from '../../styles/users/UserEdit.module.css';

type Props = {
  title?: string;
};

const UserEdit: React.FunctionComponent<Props> = () => (
  <div className={styles.box}>hi</div>
);

export default UserEdit;
